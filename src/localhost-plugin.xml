<sect1 xmlns="http://docbook.org/ns/docbook" version="5.0" xml:id="localhost-plugin">
    <title>Localhost</title>
	<info>
	</info>
	<sect2 xml:id="localhost-introduction">
	        <title>Introduction</title>
            <para>
				DDS's localhost plug-in is capable to deploy DDS agents on a local machine. Unlike SSH plug-in, localhost plug-in doesn't require a password-less access (public key, ssh agent, etc.).
                The configuration file is not required for localhost plug-in. The plug-in spawns 1 agent with a defined number of task slots on the local machine only. Just use <link linkend="dds-submit">dds-submit --slots X</link>, where X is a desired number of task slots.
            </para>
	</sect2>

	<sect2 xml:id="localhost-examples">
	  <title>Example usage</title>
    <para>
				Call using a local system only to spawn 1 DDS agent with 10 task slots:
				<screen><command>dds-submit -r localhost --slots 10</command></screen>
		</para>
	</sect2>

</sect1>
